#include <string>
#include <vector>
#include <iostream>

using namespace std;

int solution(vector<string> cells) {
    int answer = 0;
    return answer;
}

int main() {
    vector<string> cells1 = {"AAA", "AAA", "AAA", "AAA"};
    cout << solution(cells1) << endl;

    vector<string> cells2 = {"BAA", "ABB", "ABB", "AAA"};
    cout << solution(cells2) << endl;

    return 0;
}