#include <iostream>
#include <vector>

using namespace std;

int solution(vector<int> numbers, int K) {
    int answer = -1;

    
    return answer;
}

int main() {
    vector<int> numbers = {10, 40, 30, 20};
    int K = 20;
    cout << "result : " << solution(numbers, K) << endl;

    vector<int> numbers2 = {3, 7, 2, 8, 6, 4, 5, 1};
    int K2 = 3;
    cout << "result : " << solution(numbers2, K2) << endl;

    return 0;
}